# 텔레그램 알림 연결

1. 앞서 공유한 봇 토큰을 BotFather에서 폐기하고 새로 발급합니다. 채팅이나 코드에 붙이지 않습니다.
2. GitHub 저장소 Settings → Secrets and variables → Actions → New repository secret에서 다음 두 항목을 등록합니다.
   - TELEGRAM_BOT_TOKEN: 재발급한 봇 토큰
   - TELEGRAM_CHAT_ID: 메시지를 받을 본인 대화방 ID
3. 텔레그램에서 해당 봇 대화방을 열고 시작(Start)을 누릅니다.
4. Actions → 보유 종목 점검 → Run workflow. 수집·리포트 배포 후 첫 연결 확인 메시지가 옵니다.

공공데이터/Twelve Data에서 이미 수집한 완결 일봉을 사용합니다. 별도 야후 호출은 추가하지 않습니다. 공공데이터 발표 지연과 GitHub 예약 지연은 그대로 적용됩니다.

## 알림 규칙
- 첫 실행, 새 종목, 변경한 기준가는 기준 상태만 저장합니다. 이미 발생한 조건을 한꺼번에 알리지 않습니다.
- 이탈가·목표가·미실행 분할매도가: 조건 밖에서 안으로 전환할 때 개별 알림.
- 20일선 전환·252거래일 고점 돌파/돌파선 복귀: 한국 날짜 하루 한 번 변화 요약. 같은 날 추가 실행에서 생긴 요약 신호는 별도 재전송하지 않습니다.
- 같은 시세 기준일 재실행과 조건 유지 중에는 중복을 억제합니다. 회복 후 재진입은 새로운 알림입니다.
- 시세 기준 시각이 5일을 넘으면 알림을 보류합니다. 오래된 값을 최신 시세로 전송하지 않습니다.
- 가격 단위·종가 기준일을 메시지에 포함합니다. 실시간 푸시·주문 기능이 아닙니다.

## 앱과 기준값 동기화
알림은 GitHub Secret HOLDINGS_JSON을 기준으로 합니다. 앱에서 수량·이탈가·목표가·분할매도 계획을 수정했다면 앱의 데이터·설정 → 전체 백업으로 JSON을 내려받아 HOLDINGS_JSON을 교체하세요. 앱 저장만으로 GitHub Secret이 바뀌지 않습니다.
키가 없으면 알림만 건너뛰고 기존 수집·리포트는 정상 실행합니다. 알림 오류도 이미 배포한 리포트에 영향을 주지 않습니다.

## 개인정보와 전달 한계
- 봇 토큰·대화방 ID는 GitHub Secrets에서만 읽습니다. 로그에 종목명·토큰·메시지를 출력하지 않습니다.
- 중복 확인 상태는 state/alerts.enc에 암호화해 저장하며 공개 Pages에 포함하지 않습니다.
- 텔레그램에는 종목명·기준가·종가가 전송됩니다. 수량·평단·계좌번호는 메시지에 넣지 않습니다.
- 성공한 전송은 건별 저장합니다. 다만 전송 성공 직후 실행이 강제 종료되거나 상태 커밋이 실패하면 재실행 시 중복될 수 있습니다. 정확히 한 번 전달을 보장하지 않습니다.
- 암호화 상태를 읽지 못하면 알림을 멈춥니다. 비밀번호 변경 시 이전 비밀번호로 상태를 복호화해 재암호화하거나, 재기준 등록을 의도한 경우에만 상태 파일을 초기화하세요.
- 기존 Apps Script 알림과 동시에 켜면 별개로 메시지가 올 수 있습니다. 새 연결 확인 후 기존 시세 알림 트리거를 중지하면 됩니다.
