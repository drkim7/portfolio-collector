# Work 인수인계 — 2단계: 조건부 손실복구 계획 (2026-09-22)

## 목적
v19의 손실복구 코치와 `오늘 확인 큐`를 그대로 활용해서, 사용자가 물타기 금액만 저장하는 것이 아니라 **각 추가매수 단계에 실행 조건을 미리 정하고, 조건 충족 여부를 앱이 설명해 주는 구조**로 만든다.

이 기능은 매수 추천/자동 주문이 아니다. 사용자가 스스로 정한 복구 계획을 지키도록 돕는 체크리스트다.

## 현재 운영 기준
- 현재 운영본: dh-portfolio-desk v19 이상 최신 배포본.
- 기존 59개 보유자료, spotPrice/closePrice 분리, 손실복구 코치, 오늘 확인 큐, 백업/복원, collector/Apps Script 연결을 보존한다.
- Google/Google Sheets/Apps Script는 수정하지 않는다.
- 오래된 desk.tsx, seed, 과거 ZIP으로 전체 덮어쓰기 금지.

## 핵심 UX
손실 종목 상세의 `손실복구 코치` 안에 `내 복구 계획` 섹션을 둔다.

### 1. 총 추가자금 한도
- 사용자가 직접 `총 추가자금 한도`를 입력한다.
- 기존 30% / 30% / 40% 3단계 배분은 기본값으로 유지하되, 사용자가 각 단계 비율 또는 금액을 수정할 수 있게 한다.
- 1~3단계 합계는 총 한도를 넘을 수 없다.
- 3단계를 모두 사용한 뒤 4차 물타기를 자동 제안하지 않는다. `추가매수 단계 소진 · 투자논리 재검토`로 표시.

### 2. 각 단계에 실행 조건 저장
각 단계에는 금액/비율과 함께 1~4개의 `실행 조건`을 저장할 수 있게 한다.

지원할 조건 유형은 현재 앱이 이미 계산 가능한 값부터 시작한다:
- `최근 저점 유지`: 완결 일봉 기준 최근 저점을 새로 깨지 않음
- `20일선 회복`: 완결 종가가 SMA20 위
- `60일선 회복`: 완결 종가가 SMA60 위
- `RSI 회복`: 예: RSI가 사용자 지정 기준 이상 또는 과매도 구간 탈출
- `MACD 개선`: MACD histogram이 직전 완결 일봉보다 개선 또는 사용자 선택한 bullish 조건
- `거래량 확인`: 완결 일봉 거래량 배수가 사용자 지정 기준 이상
- `투자 논리 유효`: 기존 thesis 필드가 valid
- `자금조달 위험 허용`: 기존 financing/runway 기준이 사용자 계획에서 허용한 상태
- `추가매수 한도 여유`: 종목/테마/고변동성 한도 내
- `이탈선 위`: 현재 확정 기준가가 이탈선 위

기술 조건은 **완결 일봉(closePrice/histories)** 로만 확정 판정한다. 장중 spot이 조건을 잠깐 넘었다면 `장중 참고`일 뿐 `조건 충족`으로 확정하지 않는다.

### 3. 조건 상태는 3가지
각 조건을 반드시 아래 3상태로 표시:
- ✅ 충족
- ❌ 미충족
- ⚪ 확인 불가 / 자료 부족

`자료 부족`을 `미충족`으로 합치지 않는다.

### 4. 단계 상태
각 추가매수 단계는:
- `대기`: 조건 미충족 또는 확인 불가
- `확인 가능`: 사용자가 정한 모든 필수 조건 충족
- `사용 기록됨`: 사용자가 직접 실행했다고 기록한 상태

`확인 가능`을 `매수 추천`, `매수 적기`, `지금 사세요` 같은 문구로 표현하지 않는다.

### 5. 사용자가 직접 실행 기록
앱에는 실제 주문 기능을 넣지 않는다.
`사용 기록` 버튼을 누르면 확인 모달:
- 실제 추가매수 금액
- 실제 매수가
- 수량(선택/자동 계산 가능)
- 날짜/시각
- 메모

기록 후 새 평단/수량을 자동 계산하되 **보유 수량·평단을 즉시 바꿀지는 사용자가 명시적으로 확인**해야 한다.
가능하면 `계획 기록만 저장`과 `보유내역에도 반영`을 구분한다.

### 6. 실행 전 What-if
단계 카드에서 실행 전에 현재 계획금액 기준으로:
- 예상 새 평단
- 새 평단까지 필요한 상승률
- 종목 비중 변화
- 주요 태그/테마 비중 변화
- 현재가에서 추가 -20% / -30% 하락 시 예상 추가 손실

을 보여준다.
계산은 기존 손실복구 코치의 계산 로직을 재사용하고 중복 구현을 최소화한다.

## 오늘 확인 큐와 연결
v19의 C. `계획 실행 확인` 영역에 다음 이벤트를 연결한다.

- 어떤 단계가 처음으로 `대기 → 확인 가능` 전환
- 회수 기준가 도달
- 사용자가 정한 조건이 충족됐다가 다음 완결 일봉에서 다시 미충족으로 변경
- 3단계 모두 사용 기록됨

카드 문구 예:
`오가노이드사이언스 · 1차 계획 조건 3/3 충족 · 직접 확인 필요`
`20일선 회복 / RSI 기준 충족 / 한도 여유`

동일 조건이 계속 충족 중이라는 이유로 매일 새 이벤트를 만들지 말고 **상태 전환 시에만** `오늘 바뀐 것/계획 실행 확인`에 올린다.

## 상세 화면 배치
손실복구 코치 안에서 순서는 다음:
1. 현재 복구 단계와 쉬운 설명
2. 현재가 / 확정종가 / 기술분석 기준일
3. `내 복구 계획` 1~3단계
4. What-if 위험 계산
5. 회수 기준가 / 회수 비중
6. 투자 논리 및 재검토 조건

모바일에서는 각 단계 카드를 접을 수 있게 하고, 기본 상태에서는:
`1차 · 100만원 · 조건 2/3 · 대기`
처럼 한 줄 요약만 보인다.

## 데이터 모델
기존 Item/plan 모델을 우선 사용하고 optional field로 확장한다. 예시 개념:

recoveryPlan: {
  totalBudget?: number,
  tranches?: [
    {
      id: string,
      amount?: number,
      ratio?: number,
      conditions?: RecoveryCondition[],
      status?: 'waiting'|'reviewable'|'recorded',
      execution?: { at, price, amount, qty?, note?, appliedToHolding? }
    }
  ],
  recoveryPrice?: number,
  recoveryRatio?: number
}

정확한 타입명은 현재 v19 모델을 우선하며 기존 필드가 있으면 새 구조를 중복 생성하지 않는다.

백업/복원과 normalize는 새 필드가 없어도 정상 작동해야 한다. 과거 JSON은 자동 기본값으로 읽는다.

## 판정 원칙
- 확정 기술조건: histories 마지막 완결 일봉만.
- spotPrice: 평가액/장중 참고/가격 기준 도달 표시용.
- stale spot은 조건 계산에서 제외.
- 실패한 collector 데이터는 이전 값을 새 값처럼 사용하지 않는다.
- 조건 계산에 필요한 데이터가 없으면 `확인 불가`.
- 종목/테마 한도는 현재 앱의 limitsFor/exposures 로직 재사용.

## 회귀 방지
- 기존 손실복구 코치 단계 판정을 변경하지 않는다. 이 기능은 `사용자 계획` 레이어다.
- 기존 매도계획/회수기준과 충돌하지 않는다.
- 59종목 보유자료, 메모, 오늘 큐, 백업/복원, Apps Script/collector 연결 보존.
- 자동매수/자동매도/브로커 주문 기능 금지.

## 테스트
1. 과거 백업(JSON, recoveryPlan 없음)이 정상 로드된다.
2. 3단계 금액 합계가 총 한도를 넘으면 저장 차단 또는 명확한 오류.
3. 완결 일봉으로 20일선 조건이 충족되면 `대기 → 확인 가능` 전환.
4. 장중 spot만 20일선을 넘고 확정 종가가 아래면 `장중 참고`, 단계는 대기 유지.
5. RSI/MACD/거래량 데이터 누락 시 `확인 불가`, 실패로 처리하지 않음.
6. `사용 기록` 후 되돌리기/수정 가능.
7. 보유내역 반영을 선택하지 않으면 qty/buy가 바뀌지 않음.
8. 보유내역 반영을 명시하면 새 평단/수량 계산이 정확함.
9. 단계 전환 이벤트가 오늘 큐 C에 1회만 표시되고 매일 중복되지 않음.
10. 기존 전체 회귀 테스트, TypeScript, production build 통과.

## 완료 보고
- 실제 배포 버전 번호
- 수정 파일
- 테스트 결과
- 기존 59종목/백업/복원/spot-close/collector/Apps Script가 보존됐는지
- Google 관련 작업을 하지 않았는지
를 요약한다.