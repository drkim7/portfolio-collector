# Work 구현 지침 — v21 주간 리뷰 + 투자 판단 기록

작성일: 2026-09-22. 대상: 현재 배포된 dh-portfolio-desk 최신 버전(v20 이상).

## 목적
매일의 시세·기술지표·손실복구 코치·오늘 확인 큐를 사용자가 실제로 활용하도록, (1) 일주일에 한 번 객관적 변화를 요약하는 `주간 리뷰`, (2) 사용자가 자신이 왜 관찰/추매/매도 등을 선택했는지 남기는 `판단 기록`을 연결한다. 투자 추천·예측·성과 점수·자동주문을 만들지 않는다.

## 절대 원칙
- 현재 운영 앱의 가장 최신 소스를 확인한 다음 수정한다. 오래된 v19 소스, Library desk.tsx, seed, ZIP을 전체 덮어쓰지 않는다.
- v20의 59개 보유자료, 오늘 확인 큐 A~D, 조건부 복구 계획(30/30/40), 실거래 기록/보유내역 반영 선택, 손실복구 코치, spotPrice/closePrice, 암호화 collector, Apps Script 시세, 백업/복원, 사용자별 비공개 접근 모두 보존한다.
- Google/Google Sheets/Apps Script에는 로그인·수정·재배포하지 않는다. GitHub collector의 pipeline.py, portfolio-quotes.gs, Actions 스케줄도 이번 작업에서 수정하지 않는다.
- 아래 기능 중 현재 앱에 이미 구현된 기록이나 changeLog가 있다면 새로 중복 생성하지 않고 재사용·확장한다. 구현 전에 데이터 모델과 기존 화면을 먼저 점검한다.

## A. 판단 기록 (종목 상세 및 오늘 큐에서)
사용자가 카드/종목 상세에서 `판단 기록`을 추가할 수 있게 한다. 판단 종류는 `관찰 유지`, `추가매수 검토`, `추가매수 실행`, `회수/분할매도 검토`, `매도 실행`, `이탈선/목표가 수정`, `투자논리 재검토`, `기타`를 기본값으로 제공한다. 이 기록은 실제 거래내역이 아니며 `실행` 유형을 골랐더라도 실제 수량/평단을 자동 변경하지 않는다. 기존 v20 `사용 기록`에서 작성된 실행 정보는 연결 링크로 보여주되 중복 거래로 계산하지 않는다.

기본 입력은 (a) `내가 확인한 사실`, (b) `내가 선택한 행동/보류 이유`, (c) `내 생각이 틀렸음을 보여줄 조건`, (d) `다음 확인할 날짜(선택)`의 4개 짧은 항목. 빈칸을 강제로 길게 쓰게 하지 말고 1~2문장으로 저장 가능하게 한다. 날짜가 오면 앱 안에서만 확인 큐 C에 띄우고, 외부 자동 알림은 이번 버전에서 만들지 않는다.

저장 시점의 스냅샷을 명확히 표시: ticker+앱 Item 고유 ID+계좌, 사용자가 확인한 spotPrice/spotAsOf(있을 때만), 마지막 closePrice/closeDate, 당시 손실복구 단계, RSI/MACD 등 당시 존재하는 검증된 지표의 기준일. 당시 가격이 stale/missing이면 숫자를 꾸며내지 말고 해당 상태를 저장한다. 나중에 재조회한 최신 가격을 과거 기록의 가격처럼 덮어쓰지 않는다.

기록 추가/수정/삭제, 태그/종목 필터, 시간순 보기를 제공한다. 삭제 전 확인을 받고 v20 저장소의 revision/CAS 충돌 제어를 유지한다. 기록을 비공개 사용자별 데이터에 저장하며 공개 저장소, 로그, 텔레그램에는 판단기록을 전송하지 않는다.

## B. 주간 리뷰 (오늘 탭의 접이식 요약 + 별도 기록 탭 또는 같은 영역)
기본 주기는 월요일~일요일 KST 캘린더 주. 사용자가 `지난주 리뷰 생성/새로고침`을 누르면 지난 완료 주간을 생성한다. 자동 배치·별도 Google 트리거는 필요 없다. 첫 생성에서는 이전 기준 데이터가 없는 비교항목을 `첫 주 기준 수집/비교 불가`로 표시한다. 리뷰는 날짜범위와 generatedAt, 각 시장별 최신 확정 데이터 기준일을 명시한다.

상단에는 다음 5개 짧은 카드만 보여주고 나머지는 접는다:
1. `이번 주 변화`: 실제 비교 가능한 완결 일봉 기준 주요 상태 전환 건수(복구단계, 이평선, RSI 구간, MACD 등 v20 기존 규칙 재사용). 변화가 확인되지 않으면 `변화 없음`, 직전 데이터가 없으면 `비교 불가`.
2. `내 계획`: 이번 주 복구계획 조건 처음 충족·철회, 회수계획 도달, 사용자가 기록한 실행/보류 건수. 조건 유지 상태를 매일 새 이벤트처럼 누적하지 않는다.
3. `위험/한도`: 주간 마감 기준 한도 초과/해소, 이탈선 침범/복귀. 위험한도는 사용자의 기존 설정값을 재사용하고 평가액 기준일 및 환율을 명시한다.
4. `데이터 건강`: 종목별 KR/US/crypto 일봉 누락, stale spot, 지표 계산 불가 건수. 별도 중립 영역에서 보고하며 위험신호와 혼합하지 않는다.
5. `내 판단 기록`: 이번 주 작성한 판단 기록, 확인 예정일 도래, 지난주 세운 가정 중 체크할 것. 비공개 저장.

주간 리포트에는 `내가 이번 주 확인한 사실`, `내가 실행하거나 보류한 행동`, `다음 주 확인할 조건`을 사실과 사용자 기록에서만 생성한다. AI 요약이 없더라도 규칙 기반 문장으로 충분해야 한다. 근거 없는 예측/매매 추천이나 포지션 순위는 출력하지 않는다.

### 주간 데이터의 정확성
- 주간 가격 변화는 같은 종목의 동일 출처/동일 조정 기준 완결 종가가 양쪽 기준일에 존재할 때만 계산한다. KR/US는 각 시장 거래일, crypto는 24/7 UTC 일봉을 구분한다. UTC 일봉은 다음 UTC 날짜가 시작하기 전에는 완결되지 않는다.
- 전주 대비 가격률은 `가격 변화`로 명시한다. 매매·입출금·환율·분할·배당 효과를 분리할 수 없다면 `실현/총 수익률`이라고 표시하지 않는다. 포트폴리오 총수익률도 정확한 현금흐름·보유변동 자료 없이는 산출하지 않는다.
- 종목의 보유수량/평단 또는 계좌가 주중 변경되었다면 손익 변화를 단순 투자성과로 비교하지 않는다. 사용자 거래 기록이 없으면 `보유변동 가능성/비교 제한`.
- 비교 가능한 확정 바가 부족하면 해당 통계는 `자료 부족`. 주간 리뷰를 일부만 만들 수 있지만 누락 종목 수와 데이터 기준을 보여준다.
- 장중 spot은 주간 기술지표·확정 단계 계산에 사용하지 않는다. 별도의 현재 상태 참고만 가능.
- 기존 주간 리뷰 스냅샷이 없다면 과거 상태 전환·확정 시점을 추측하지 않는다. 확보된 histories로 재계산 가능한 항목만 날짜를 명시해 표시하고 사용자 기록은 작성시점부터 쌓는다.

### 리뷰 저장과 탐색
주 단위 ID(예: 2026-W39)와 생성 시각, 기준 일봉 날짜, 주요 집계, 연결된 판단기록 ID를 저장한다. 같은 주에 리뷰를 다시 생성하면 새 스냅샷 생성/기존 리뷰 수정 여부를 명확히 표시하고, 사용자가 작성한 주간 회고 텍스트는 절대 자동 덮어쓰지 않는다. 지난 12주 목록 + 주별 상세 열람, 과거 주간 리뷰를 임의 삭제하지 않도록 한다. 보고서 숫자를 계산할 때 현재 보유자료를 과거 특정 시점 데이터처럼 사용하지 않는다.

## 화면 배치
- `오늘` 탭 기존 A/B/C/D 큐의 순서와 동작은 보존한다.
- 큐 위 또는 큐 바로 아래에 높이가 낮은 `지난주 한눈에` 요약 카드 하나만 추가: 상태 전환, 계획 점검, 데이터 누락, 작성한 판단 기록 숫자 + `전체 리뷰` 버튼.
- 별도의 `기록·리뷰` 진입점은 기존 탭 수와 현재 반응형 구조를 검토한 뒤 추가하거나 `오늘` 탭 하위로 구현한다. 모바일에서는 큰 차트보다 한 줄 변화, 근거 확인, 바로 판단 기록 버튼을 우선한다.
- 종목 상세에서 `내 복구 계획` 바로 아래에 `판단 기록` 요약(최근 2~3건)과 `기록 추가` 버튼을 넣는다.
- 기록/리뷰로부터 기존 종목 상세·복구코치로 되돌아가는 링크를 제공한다.

## 데이터/스토리지
기존 사용자별 portfolio 저장소를 재사용한다. 새로운 optional journal/reviews 필드를 쓰면 normalize, API 검증, CAS, JSON 내보내기/복원에 모두 포함한다. 필요하지 않은 DB 마이그레이션은 하지 않는다. 백업 스키마 버전이 필요하면 구버전 JSON을 무손실 복원한다. 최대 파일 크기/기록 개수와 서버 payload 제한을 검토해 기록이 많아져도 기존 포트폴리오 저장이 실패하지 않게 한다.

## 검증/완료 기준
1. v20 실제 최신 소스 위에서만 작업하고 이전 v20 기능을 덮어쓰지 않는다.
2. 판단 기록 CRUD, 기록 시점의 spot/close 기준시각 스냅샷, stale/missing 처리, CAS 충돌, 사용자 분리 테스트.
3. 실제 추가매수 기록과 판단 기록이 중복 매매 계산을 일으키지 않는다.
4. 59종목 백업·복원 왕복에서 기존 필드 + 신규 journal/reviews 필드가 모두 보존된다.
5. 주간 비교 불가/신규등록/중간 매매/이력 부족/crypto UTC 주말 등 경계 사례 테스트.
6. 같은 주 새로고침이 판단 기록·수기 회고를 덮어쓰지 않는다.
7. 기존 A/B/C/D 큐, v20 조건부 복구계획, spot/close, Google spot feed, 암호화 collector 연결 회귀 테스트.
8. TypeScript 검사, 기존 전체 테스트, 변경 파일 ESLint, 프로덕션 빌드 통과. 모바일/태블릿에서 핵심 카드 수평 오버플로가 없는지 확인.
9. 실제 최신 사이트에 배포한 버전과 수정 파일, 검증된 항목/검증 불가 항목을 구분해 보고한다.

Google Apps Script와 GitHub collector 코드 변경은 이번 버전의 범위 밖이다. 그쪽 변경이 꼭 필요하다고 판단되면 실제로 수정하지 말고 이유·요청 데이터 계약·최소 변경안을 문서로 남겨 사용자에게 별도 안내한다.