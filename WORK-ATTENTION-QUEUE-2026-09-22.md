# Work 인수인계 — 1단계: 오늘 확인 큐 재배치 (2026-09-22)

## 목적
현재 앱에 이미 있는 시세·확정일봉·손실복구 코치·이탈선·매도계획·태그한도·수집상태를 새 기능처럼 중복 구현하지 말고, `오늘` 탭에서 사용자가 59종목 중 먼저 볼 항목을 자동으로 추려주는 `오늘 확인 큐`로 재배치한다.

## 가장 중요한 작업 원칙
- **현재 운영 중인 dh-portfolio-desk의 최신 배포본 소스를 기준으로 수정한다.** Library의 9/21 desk.tsx나 오래된 handoff ZIP으로 전체 덮어쓰기 금지.
- 기존 59개 보유자료, 메모, 복구계획, 매도계획, 백업/복원, spotPrice/closePrice 분리, collector 연결, Apps Script 연결을 보존한다.
- 매수·매도 추천 점수나 종목 랭킹을 만들지 않는다. 이 큐는 `투자 매력도`가 아니라 `오늘 확인 필요성`만 정렬한다.
- 자료 부족은 위험 신호와 분리한다. `나쁨 / 확인 필요 / 자료 없음`을 구분한다.
- 실제 주문 기능은 추가하지 않는다.

## 오늘 탭 목표 구조
`오늘` 탭의 최상단을 다음 4개 영역으로 구성한다. 전체 종목 카드는 아래로 내리거나 접는다.

### A. 지금 확인
가장 긴급한 확인 항목. 다음 중 하나라도 충족하면 포함:
- 이탈선 침범 또는 이탈선까지 거리가 매우 가까움(기존 앱의 stop/stopDistance 판정 재사용)
- 목표가/분할매도 단계 도달
- 장중 또는 확정종가 기준 큰 변동(기본 5%, 가능하면 사용자 기존 알림 기준 재사용)
- 복구 코치 단계가 이전 저장값 대비 악화/개선
- 태그/종목 한도 초과 또는 추가매수 한도 0

표시 예:
`오가노이드사이언스 · 장중 +14.5% · 복구단계 1 유지 · 현재가 7,100 / 확정종가 6,200`

### B. 오늘 달라진 것
어제/직전 collector 상태 대비 의미있는 기술 변화만 표시:
- 20/60/120일선 상향/하향 교차 또는 가격의 이평선 재진입/이탈
- RSI 구간 변화(예: <30 탈출, 50 상향/하향 등 기존 규칙이 있으면 재사용)
- MACD 방향 변화/골든·데드 등 기존 계산값 변화
- 거래량 배수 임계치 돌파
- 52주/252봉 돌파 최초 발생·유지·이탈
- 손실복구 코치 단계 변화

절대 조건: 현재 값만 보고 `변화`라고 부르지 말고, 저장된 직전 상태 또는 collector가 제공하는 직전 상태와 비교 가능한 경우에만 표시한다. 비교 자료가 없으면 `변화 확인 불가`.

### C. 계획 실행 확인
- 분할매도 계획가 도달
- 회수 기준가 도달
- 추가매수 단계의 사전조건이 충족되었지만 아직 사용자가 실행 완료로 기록하지 않은 경우
- 사용자가 저장한 계획 중 기한/상태 확인이 필요한 경우(기존 모델에 기한 필드가 없으면 새로 만들지 않아도 됨)

주의: `매수하세요/매도하세요`가 아니라 `계획 조건 도달 · 확인 필요` 문구를 사용.

### D. 데이터 문제
- Apps Script spot stale/unavailable
- collector 일봉 실패/누락
- 일봉 수 부족으로 지표 계산 불가
- ticker 불일치/종목코드 누락
- spot은 최신인데 close/history가 오래되었거나 반대인 경우

이 영역은 위험 신호와 섞지 말고 회색/중립 스타일로 표시.

## 정렬 규칙
투자 점수 금지. 내부적으로 `attentionReason` 우선순위만 둔다.
권장 순서:
1. 이탈선 침범
2. 계획가 도달 / 한도 초과
3. 복구단계 변화
4. 5% 이상 장중/확정 변동
5. 기술 상태 변화
6. 데이터 문제

동일 우선순위에서는 최신 이벤트 시각 우선. 수익률이나 손실률이 크다는 이유만으로 상단 랭킹하지 않는다.

## 카드 최소 정보
태블릿/모바일에서 카드 한 장은 최대한 짧게:
- 종목명
- 현재가(spot) + 장중 변동률
- 최근 확정종가/기준일
- 상태 배지 1개
- `왜 이 큐에 들어왔는지` 한 줄
- 누르면 기존 상세화면으로 이동

예:
`오가노이드사이언스`
`현재 7,100원 (+14.5%) · 확정 6,200원 (9/21)`
`🟠 복구 1단계 유지`
`오늘 급등했지만 완결 일봉 기준 추세 회복은 아직 미확정`

## 특이사항 없음
오늘 큐에 포함되지 않은 종목은 `특이사항 없음 N종목`으로 접어서 보여주고, 사용자가 펼치면 기존 종목 카드/목록을 볼 수 있게 한다.

## 데이터/상태 저장
- 가능하면 별도 영구 스키마를 크게 늘리지 말고 기존 collector의 이전 실행 비교/state 또는 이미 앱에 존재하는 changeLog/recovery state를 활용.
- 꼭 필요한 경우에만 `attentionSnapshot` 같은 최소 상태를 추가하고 normalize/backup/restore와 호환시키며 기존 백업 파일이 깨지지 않게 optional 필드로 처리.
- `spotPrice`는 현재 표시/평가액용, `closePrice`/histories는 기술 판단용이라는 현재 분리를 유지.

## 테스트
반드시 다음을 검증하고 결과를 보고:
1. 59종목이 있는 기존 계정에서 오늘 탭이 로드된다.
2. 오가노이드사이언스처럼 장중 급등 + 전일 확정종가가 다른 종목이 `지금 확인`에 들어가고 두 가격이 분리 표시된다.
3. 이탈선 침범 종목이 다른 단순 급등락보다 위에 온다.
4. 데이터 stale 종목은 `데이터 문제`에만 들어가며 위험/매수 신호로 오인되지 않는다.
5. 아무 이벤트 없는 종목은 `특이사항 없음`으로 접힌다.
6. 기존 손실복구 코치/종목 상세/백업·복원/59개 보유자료가 회귀하지 않는다.
7. 타입체크/빌드/기존 테스트를 통과한다.

## 완료 기준
- 코드 수정만 말고 최신 앱에 실제 배포.
- 모바일/태블릿 화면에서 오늘 탭 확인.
- 변경 파일과 테스트 결과 요약.
- 기존 기능을 덮어쓰거나 59개 보유자료를 seed로 리셋하지 않았음을 확인.